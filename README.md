## Note

This repository contains earlier commits with environment and binary files.
These files are ignored in later commits using `.gitignore`.

The core implementation of the project is in **fr.py**.
